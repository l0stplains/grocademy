<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>{{title}}</title>
  <link rel="stylesheet" href="/static/css/styles.css" />
  <script defer src="/static/assets/js/app.js"></script>
</head>
<body>
  {{> navbar}}
  <main class="container" style="max-width:820px">
    <h1>Create your account</h1>
    {{#if error}}<div class="alert error">{{error}}</div>{{/if}}

    <form method="post" action="/register" class="card">
      <div class="form-row two">
        <label>Email <input type="email" name="email" required></label>
        <label>Username <input name="username" minlength="3" required></label>
      </div>
      <div class="form-row two">
        <label>First name <input name="first_name" required></label>
        <label>Last name <input name="last_name" required></label>
      </div>
      <div class="form-row two">
        <label>Password <input type="password" name="password" minlength="8" required></label>
        <label>Confirm Password <input type="password" name="confirm_password" minlength="8" required></label>
      </div>
      <button class="btn primary" type="submit">Sign up</button>
    </form>
    <p class="muted">Already have an account? <a href="/login" style="color:var(--blue)">Login</a></p>
  </main>
  {{> footer year="2025"}}
</body>
</html>